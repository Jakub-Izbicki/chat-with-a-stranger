{
  "name": "chatwithastranger-boot",
  "version": "0.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "postinstall": "npm install --prefix ./client && npm install --prefix ./server",
    "start": "npm run build --prefix ./client && npm run start --prefix ./server",
    "heroku:deploy": " git push heroku master && heroku logs --tail"
  },
  "author": "",
  "license": "ISC"
}
